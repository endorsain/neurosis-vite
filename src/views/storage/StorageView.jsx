import { PageLayoutBody, ViewLayout } from '../../components';

const StorageView = () => {
  return (
    <PageLayoutBody.View viewName="StorageView" sizeView="small">
      <ViewLayout placeholder="Storage view">Contenido storage</ViewLayout>
    </PageLayoutBody.View>
  );
};

export default StorageView;
